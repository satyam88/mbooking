apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-mbooking-ingress
  namespace: prod
  labels:
    env: prod
    app: mbooking
    layer: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: prod-mbooking.connectingclouds.in
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: prod-mbooking-service
                port:
                  number: 8080